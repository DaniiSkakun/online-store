PORT=5000
SECRET_KEY=super-secret-key
CLIENT_URL=http://localhost:3000

# --- Database (PostgreSQL on Render) ---
DB_HOST=localhost
DB_PORT=5432
DB_NAME=online_store
DB_USER=postgres
DB_PASSWORD=postgres
USE_SQLITE=0
# Optionally, instead of the vars above you can set DATABASE_URL=postgres://USER:PASSWORD@HOST:PORT/DB_NAME

# --- Mailer (SMTP) ---
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=

# Alternative key names supported by the code (leave blank if not used)
MAIL_HOST=
MAIL_PORT=
MAIL_USER=
MAIL_PASSWORD=
MAIL_FROM=
EMAIL_HOST=
EMAIL_PORT=
EMAIL_USER=
EMAIL_PASSWORD=
EMAIL_FROM=
